---
# The controller runs Ansible from this repository. This file does not imply
# that the repository exists on the PaperCut server.
all:
  children:
    papercut_mf:
      hosts:
        papercut-mf:
          ansible_host: 10.1.0.113
          ansible_user: CHANGE_ME
          ansible_become: true
